# nested-units

    Code
      start_design(name = "nested units") %>% set_units(block = 3, plot = nested_in(
        block, 2))
    Output
      nested units
      \-block (3 levels)
        \-plot (6 levels)

